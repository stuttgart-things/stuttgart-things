---
apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  name: bootstrap-flux-cluster-43
  namespace: tekton
spec:
  pipelineRef:
    resolver: git
    params:
      - name: url
        value: https://github.com/stuttgart-things/stuttgart-things.git
      - name: revision
        value: main
      - name: pathInRepo
        value: stageTime/pipelines/bootstrap-flux-cluster.yaml
  # params:
  #   - name: kubeconfigString
  #     value: $(tasks.getting-secrets.results.array-results[0])
  #   - name: githubToken
  #     value: $(tasks.getting-secrets.results.array-results[1])