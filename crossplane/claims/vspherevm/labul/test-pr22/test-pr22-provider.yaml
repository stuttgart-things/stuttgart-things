---
apiVersion: tf.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: test-pr22
  namespace: crossplane-system
spec:
  configuration: |
    terraform {
      backend "kubernetes" {
        secret_suffix     = "test-pr22"
        namespace         = "crossplane-system"
        in_cluster_config = true
      }
    }