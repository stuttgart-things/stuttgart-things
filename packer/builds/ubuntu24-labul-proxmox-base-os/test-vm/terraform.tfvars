vsphere_server="{{ .server }}"
vsphere_user="{{ .user }}"
vsphere_password="{{ .password }}"
vm_ssh_user="{{ .sshUser }}"
vm_ssh_password="{{ .sshPassword }}"

# password=cloud/data/pve:api_password, sshUser=cloud/data/pve:ssh_user, sshPassword=cloud/data/pve:ssh_password, server=cloud/data/pve:api_url, user=cloud/data/pve:api_user
