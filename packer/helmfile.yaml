---
releases:
  - name: packer
    namespace: machine-shop-packer
    chart: stuttgart-things/sthings-cluster
    version: 0.3.7
    installed: true
    values:
      - "values/{{`{{ .Release.Name }}`}}.yaml.gotmpl"

  - name: ansible
    namespace: machine-shop-packer
    chart: stuttgart-things/sthings-cluster
    version: 0.3.7
    installed: true
    values:
      - "values/{{`{{ .Release.Name }}`}}.yaml.gotmpl"

  - name: secrets
    namespace: machine-shop-packer
    chart: stuttgart-things/sthings-cluster
    version: 0.3.7
    installed: true
    values:
      - "values/{{`{{ .Release.Name }}`}}.yaml.gotmpl"

  - name: job
    namespace: machine-shop-packer
    chart: stuttgart-things/sthings-cluster
    version: 0.3.7
    installed: true
    values:
      - "values/{{`{{ .Release.Name }}`}}.yaml.gotmpl"
    needs:
      - "{{`{{ .Release.Namespace }}`}}/secrets"
      - "{{`{{ .Release.Namespace }}`}}/packer"
      - "{{`{{ .Release.Namespace }}`}}/ansible"

repositories:
  - name: stuttgart-things
    url: eu.gcr.io/stuttgart-things
    oci: true

helmDefaults:
  verify: false
  wait: false
  timeout: 600
  recreatePods: false
  force: true

environments:
  labda-vsphere:
    values:
      - environments/defaults.yaml
      - environments/{{ .Environment.Name }}.yaml
  labul-vsphere:
    values:
      - environments/defaults.yaml
      - environments/{{ .Environment.Name }}.yaml
