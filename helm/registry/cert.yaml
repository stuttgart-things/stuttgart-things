---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kibana-ingress
  namespace: elastic-system
spec:
  commonName: kibana.app-dev.sthings-vsphere.labul.sva.de
  dnsNames:
  - kibana.app-dev.sthings-vsphere.labul.sva.de
  issuerRef:
    kind: ClusterIssuer
    name: vault-approle
  secretName: kibana-kibana-tls
