---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: registry-ingress
  namespace: registry
spec:
  commonName: registry.app-dev.sthings-vsphere.labul.sva.de
  dnsNames:
    - registry.app-dev.sthings-vsphere.labul.sva.de
  issuerRef:
    kind: ClusterIssuer
    name: vault-approle
  secretName: registry-tls
