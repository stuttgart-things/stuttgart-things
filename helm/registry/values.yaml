---
persistence:
  accessMode: 'ReadWriteOnce'
  enabled: false
  size: 10Gi
  storageClass: nfs-csi

ingress:
  enabled: true
  className: nginx
  path: /
  hosts:
    - registry.app-dev.sthings-vsphere.labul.sva.de
  annotations:
    nginx.ingress.kubernetes.io/secure-backends: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: 500m
  tls:
    - secretName: registry-kibana-tls
      hosts:
        - registry.app-dev.sthings-vsphere.labul.sva.de

secrets:
  haSharedSecret: ""
  htpasswd: |-
    sthings:$2y$05$CrDbu7Cb7R9ZwF4HGmlFcOWIpW/W4OruCU.qbZYN2T/ZhTC0YW/ei

# helm repo add twuni https://helm.twun.io
# helm repo update
# helm upgrade --install registry twuni/docker-registry --version 2.2.3 --values registry/values.yaml -n registry --create-namespace
