---
secrets:
  sthings-admin-password:
    namespace: awx
    kvs:
      password: ref+vault://apps/awx/password-dev
  sthings-custom-certs:
    namespace: awx
    kvs:
      bundle-ca.crt: ref+vault://apps/awx/cabundle

  # caBundle:
  #   create: true
  #   secretName: sthings-custom-certs

instances:
  dev:
    name: awx-dev
    namespace: awx
    adminUser: ref+vault://apps/awx/user-dev
    adminPassword: sthings-admin-password
    hostname: awx-dev
    domain: homerun-dev.sthings-vsphere.labul.sva.de
    ingressClassName: nginx
    ingressSecret: awx-dev
    postgresStorageLimits: 8Gi
    postgresStorageRequest: 1Gi
    projectPersistence: false
    projectsStorageAccessMode: ReadWriteMany
    fsGroupChangePolicy: OnRootMismatch
    bundleCacertSecret: sthings-custom-certs
  # prod:
  #   name: awx
  #   namespace: awx
  #   adminUser: ref+vault://apps/awx/user-prod
  #   adminPassword: ref+vault://apps/awx/password-prod
  #   hostname: awx
  #   domain: homerun-dev.sthings-vsphere.labul.sva.de
  #   ingressClassName: nginx
  #   ingressSecret: awx
  #   postgresStorageLimits: 8Gi
  #   postgresStorageRequest: 1Gi
  #   projectPersistence: false
  #   projectsStorageAccessMode: ReadWriteMany
  #   fsGroupChangePolicy: OnRootMismatch
  #   bundleCacertSecret: sthings-custom-certs
