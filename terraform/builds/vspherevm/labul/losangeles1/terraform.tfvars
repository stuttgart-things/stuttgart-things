vsphere_server="{{ .server }}"
vsphere_user="{{ .user }}"
vsphere_password="{{ .password }}"
vm_ssh_user="{{ .sshUser }}"
vm_ssh_password="{{ .sshPassword }}"

# password=apps/data/scr:password,username=apps/data/scr:username
