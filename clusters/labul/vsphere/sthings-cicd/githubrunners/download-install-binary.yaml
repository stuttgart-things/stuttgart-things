---
apiVersion: resources.stuttgart-things.com/v1alpha1
kind: GithubRunner
metadata:
  name: download-install-binary # e.g. install-requirements
  namespace: crossplane-system # e.g. crossplane-system
spec:
  repository: download-install-binary # e.g. install-requirements
  clusterConfig: cicd # e.g. cicd