---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: awx-operator
  namespace: awx
spec:
  interval: 30m
  chart:
    spec:
      chart: awx-operator
      version: 2.5.3
      sourceRef:
        kind: HelmRepository
        name: awx-operator
        namespace: awx
      interval: 12h
  values:
    installCRDs: true