---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: secret-store-csi-driver
  namespace: vault
spec:
  interval: 30m
  dependsOn:
    - name: vault-deployment
      namespace: vault
  chart:
    spec:
      chart: secrets-store-csi-driver
      version: 1.3.4
      sourceRef:
        kind: HelmRepository
        name: secrets-store-csi
        namespace: vault
      interval: 12h

# vault login -address=https://vault.dev11.4sthings.tiab.ssc.sva.de -tls-skip-verify
