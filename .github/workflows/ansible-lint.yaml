on:
  workflow_call:

jobs:
  linting:
    runs_on: 
    continue-on-error: true
    steps:
      - uses: actions/checkout@v3

      - name: Ansible Lint
        uses: ansible/ansible-lint-action@v6.11.0
