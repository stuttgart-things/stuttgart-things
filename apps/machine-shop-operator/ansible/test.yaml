- name: Working with extra variables - Cmd Line JSON
  hosts: localhost
  connection: local
  gather_facts: no

  tasks:

  - name: CmdLine JSON - string
    debug:
      msg: "{{ item }}"
    with_dict: "{{ lookup('vars', myname) }}"
