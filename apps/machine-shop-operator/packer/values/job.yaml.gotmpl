---
jobs:
  packer-job:
    name: {{ now | date "2006-01-02"}}-{{ .Values.osVersion }}-{{ .Values.ansibleOsProvioning }}-{{ randAlphaNum 4 | lower }}
    namespace: {{ .Release.Namespace }}
    labels:
      app: machine-shop-operator
      machine-shop-operator: packer
    restartPolicy: Never
    volumes:
      ansible:
        volumeKind: configMap
      packer:
        volumeKind: configMap
    containers:
      manager:
        image: {{ .Values.packerImage }}
        tag: {{ .Values.packerImageTag }}
        imagePullPolicy: Always
        securityContext:
          runAsUser: 65532
          allowPrivilegeEscalation: false
          privileged: true
          runAsNonRoot: true
          readOnlyRootFilesystem: false
        command:
          - "/bin/sh"
          - "-c"
          - "tail -f /dev/null"
          #- packer init {{ .Values.packerConfigMountPath }}/{{ .Values.osVersion }}-{{ .Values.cloud }}.pkr.hc && packer build -force -var "username=phermann" -var "password=Atlan7is" {{ .Values.packerConfigMountPath }}/{{ .Values.osVersion }}-{{ .Values.cloud }}.pkr.hcl
        resources:
          requests:
            cpu: 10m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 768Mi
        volumeMounts:
          ansible:
            mountPath: {{ .Values.ansiblePlayMountPath }}
          packer:
            mountPath: {{ .Values.packerConfigMountPath }}
        secretsEnvFrom:
          - name: vault
