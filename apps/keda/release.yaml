
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: minio-deployment
  namespace: keda
spec:
  interval: 30m
  chart:
    spec:
      chart: minio
      version: 12.6.0
      sourceRef:
        kind: HelmRepository
        name: kedacore
        namespace: keda
      interval: 12h
